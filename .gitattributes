# Normalize line endings: LF in the repo, LF on checkout (scripts run on Linux).
* text=auto eol=lf

# Windows scripts keep CRLF
*.ps1 text eol=crlf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
